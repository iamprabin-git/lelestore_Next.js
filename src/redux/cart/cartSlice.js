import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    properties: [],
    totalPrice: 0,
};

const cartSlice = createSlice({
    name: "cart",
    initialState,
    reducers: {
        
    }
},
})